<UserControl x:Class="Academix.Views.DataSystemView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Academix.Views"
             xmlns:views="clr-namespace:Academix.Views"
             mc:Ignorable="d" 
             d:DesignHeight="770" d:DesignWidth="1380">
    <UserControl.Resources>
        <Style TargetType="Expander"
               BasedOn="{StaticResource LightBlueExpander}"/>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <DockPanel>
            
                <Expander DockPanel.Dock="Top"
                          FlowDirection="RightToLeft">
                    <Expander.HeaderTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal"
                                FlowDirection="LeftToRight"
                                Height="30">

                                <TextBlock Height="Auto"
                                    Width="Auto"
                                    FontSize="18"
                                    VerticalAlignment="Center"
                                    Foreground="White"
                                    FontWeight="Bold"
                                    Margin="10,0,100,0"><Run Text="Thông số hệ thống"/></TextBlock>
                            </StackPanel>
                        </DataTemplate>
                    </Expander.HeaderTemplate>
                    <ContentControl Margin="0,20,0,0"
                                    FlowDirection="LeftToRight">
                        <views:SystemParametersView/>
                    </ContentControl>
                </Expander>
           
            <Expander DockPanel.Dock="Top"
                
                Margin="0,20,0,20"
                      FlowDirection="RightToLeft">
                <Expander.HeaderTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal"
                            FlowDirection="LeftToRight"
                            Height="30">

                            <TextBlock Height="Auto"
                                Width="Auto"
                                FontSize="18"
                                VerticalAlignment="Center"
                                Foreground="White"
                                FontWeight="Bold"
                                Margin="10,0,10,0"><Run Text="Danh sách môn học"/></TextBlock>
                        </StackPanel>
                    </DataTemplate>
                </Expander.HeaderTemplate>
                <ContentControl Margin="0,20,0,0"
                                FlowDirection="LeftToRight">
                    <views:SubjectSystemView/>
                </ContentControl>
            </Expander>
        </DockPanel>

    </ScrollViewer>
</UserControl>
