<Window x:Class="Academix.Views.PermissionMatrixView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Phân quyền người dùng" Height="600" Width="1000"
        Background="#f1f8e9">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="PHÂN QUYỀN NGƯỜI DÙNG" FontSize="20" FontWeight="Bold"
                   Foreground="DarkGreen" HorizontalAlignment="Center" Margin="0,0,0,10"/>

        <DataGrid x:Name="PermissionMatrix"
                  Grid.Row="1"
                  AutoGenerateColumns="False"
                  ItemsSource="{Binding PermissionMatrix}"
                  CanUserAddRows="False"
                  HeadersVisibility="All"/>
    </Grid>
</Window>
